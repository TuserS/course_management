<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../../../favicon.ico">

    <title>edit profile</title>

    <!-- Bootstrap core CSS -->
    <link href="bootstrap-4.0.0-dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="css/sign.css" rel="stylesheet">
  </head>

  <body class="text-center">
    <form class="form-signin" method="post" action="/editProfile">
      <h1 class="h3 mb-3 font-weight-normal">Edit profile</h1>

      <label for="inputName" class="sr-only">Name</label>
      <input name="name" value="<%= loggedUser.name %>" type="text" id="inputName" class="form-control" placeholder="Name" required autofocus>

      <label for="inputEmail" class="sr-only">Email address</label>
      <input name="email" value="<%= loggedUser.email %>" type="email" id="inputEmail" class="form-control" placeholder="Email address" required>

      <div class="radio ">
            <label>
                <% if( loggedUser.gender == 'Male' ){ %>
                    <input type="radio" value="Male" name="gender" checked required> Male
                    <input type="radio" value="Female" name="gender"> Female
                <% } else {%>
                <input type="radio" value="Male" name="gender" required> Male
                <input type="radio" value="Female" name="gender" checked> Female
                <% } %>
            </label>
      </div>
      <br>

      <button class="btn btn-lg btn-primary btn-block" type="submit">Update profile</button>
      <a class="nav-link active" href="/home">Back to home page</a>
      <p class="mt-5 mb-3 text-muted">&copy; 2018 - Nodejs Sample Project</p>
    </form>

  </body>
</html>
